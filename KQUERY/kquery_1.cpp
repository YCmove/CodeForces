#include<bits/stdc++.h>

using namespace std;
#define _ ios_base::sync_with_stdio(false);cin.tie(NULL);



int main(){
    int n, num;
    cin >> n;
    while(n--){
        cin >> num;
        
    }
    
    int n_query;
    while(n_query--){
        int i, j, k;
        cin >> i >> j >> k;

    }

    return 0;
}